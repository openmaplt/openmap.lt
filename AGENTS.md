## Environment Setup
Before running any verification scripts, the local development server must be running:
1. **Install:** `npm install`
2. **Start & Wait:** Run `npm run dev & npx wait-on http://localhost:3000`
3. **Verification:** Once the port is active, you can execute the python (or other) verification scripts.
