@AGENTS.md

## Claude Code — additional notes

This file's content comes from `AGENTS.md` (the shared source of truth for all agents). Session-specific nuances (mistakes corrected, confirmed decisions) live separately in the auto-memory system (`/memory`) — only things too specific/transient for a repo-level file go there.
