# PostgreSQL konfigūracija
POSTGRES_USER=openmap
POSTGRES_PASSWORD=openmap
POSTGRES_DB=openmap
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Database URL (naudojamas Node.js programoje)
# Development (kai PostgreSQL veikia per docker-compose):
DATABASE_URL=postgresql://openmap:openmap@localhost:5432/openmap

# Svetainės bazinis URL (lokaliai - localhost, produkcijoje - https://openmap.lt)
BASE_URL=http://localhost:3000

# STVK API (saugomų teritorijų duomenys imami tiesiai iš kadastro)
# Nenustačius naudojamas testinis serveris https://test.stvk.lt
STVK_API_URL=https://test.stvk.lt

# Kur saugomi naudotojų įkeltos nuotraukos (src/lib/photoStorage.ts).
# Nenustačius naudojamas ./uploads (santykinai su repo šaknimi) — lokaliam dev pakanka.
# Production (Docker) šis kelias yra /app/uploads, nustatomas Dockerfile ENV, čia nereikia keisti.
# UPLOADS_DIR=./uploads

# Production (kai PostgreSQL veikia host mašinoje, o app Docker konteineryje):
# DATABASE_URL=postgresql://openmap:openmap@host.docker.internal:5432/openmap

# OSM OAuth2 (registruokite: https://www.openstreetmap.org/oauth2/applications)
OSM_CLIENT_ID=
OSM_CLIENT_SECRET=

# Google OAuth2 (registruokite: https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# SMTP (el. pašto dienos suvestinė administratoriams/moderatoriams apie
# laukiančius patvirtinimo komentarus/nuotraukas). Nenustačius SMTP_HOST —
# suvestinė tyliai nesiunčiama.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=openmap.lt <noreply@openmap.lt>

# Valanda (0-23, Lietuvos laiku), kada siunčiama dienos suvestinė. Numatyta 8.
MODERATION_DIGEST_HOUR=8

# Gemini API (AI paieška pokalbiu žemėlapio paieškos juostoje – admin-only,
# src/app/api/ai-search/route.ts). Nenustačius GEMINI_API_KEY, paspaudus AI
# paieškos mygtuką gaunama klaida (mygtukas administratoriui vis tiek
# rodomas) — likusi aplikacija veikia normaliai.
# Registruokite: https://aistudio.google.com/apikey
GEMINI_API_KEY=
